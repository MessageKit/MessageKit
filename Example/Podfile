source 'https://cdn.cocoapods.org/'

platform :ios, '12.0'

target 'ChatExample' do
  use_frameworks!
  pod 'MessageKit', :path => '../'
  pod 'SwiftLint', '~> 0.39.1'
  pod 'PINRemoteImage', :git => 'https://github.com/classdojo/PINRemoteImage', :branch => 'master', :subspecs => ['PINCache', 'iOS', 'WebP']
  pod 'PINCache', :git => 'https://github.com/pinterest/PINCache', :branch => 'master'


  target 'ChatExampleTests' do
    inherit! :search_paths
  end

  target 'ChatExampleUITests' do
    inherit! :search_paths
  end
end
